services:
  - type: web
    name: eobi-wage-calculator
    env: python
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: streamlit run app_web.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
